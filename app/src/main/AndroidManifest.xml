<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.chuangjiangx.kitxandroid">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".application.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".network.demo3.NetworkDemo3Activity" />
        <activity android:name=".network.demo2.NetworkDemo2Activity" />
        <activity android:name=".network.demo1.NetworkDemo1Activity" />
        <activity android:name=".network.index.NetworkActivity" />
        <activity android:name=".main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".print.PrintTestActivity" />
        <activity android:name=".print.PrintActivity" />
        <activity android:name=".viewnavi.NaviActivity" />
        <activity android:name=".speak.SpeakActivity" />
    </application>

</manifest>